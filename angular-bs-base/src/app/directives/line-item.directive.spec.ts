import { LineItemDirective } from './line-item.directive';

describe('LineItemDirective', () => {
  it('should create an instance', () => {
    const directive = new LineItemDirective();
    expect(directive).toBeTruthy();
  });
});
